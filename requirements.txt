# WanderLens — install everything once:
#   python -m pip install -r requirements.txt
#
# Photo labeling uses the OpenAI Python SDK (openai>=1.40) for BOTH:
#   - Paid OpenAI API  → set OPENAI_API_KEY, run with --provider openai
#   - Free GitHub Models → set GITHUB_MODELS_TOKEN, run with --provider github
# No extra package is required for GitHub Models (same SDK, different base URL).

# Core pipeline
pandas>=2,<3
numpy>=1.24
tqdm>=4,<5

# Vision labeling (OpenAI + GitHub Models)
Pillow>=10
pillow-heif>=0.16
openai>=1.40,<2
httpx>=0.23

# LDA album topics
nltk>=3.8
gensim>=4.3

# TF-IDF recommendations + Excel export
scikit-learn>=1.3
openpyxl>=3.1
xlsxwriter>=3.1
